{
  "id": "zenjex-internals",
  "titleRu": "Zenjex: устройство",
  "titleEn": "Zenjex Internals",
  "en": [
    {
      "type": "heading",
      "level": 1,
      "text": "Zenjex Internals"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Table of Contents"
    },
    {
      "type": "list",
      "items": [
        "[ProjectRootInstaller](#projectrootinstaller)",
        "[RootContext](#rootcontext)",
        "[BindingBuilder](#bindingbuilder)",
        "[ContainerBindingBuilder](#containerbindingbuilder)",
        "[IInitializable](#iinitializable)",
        "[ZenjexInjector](#zenjexinjector)",
        "[ZenjexRunner](#zenjexrunner)",
        "[ZenjexBehaviour](#zenjexbehaviour)",
        "[SceneInstaller](#sceneinstaller)",
        "[ZenjexSceneContext](#zenjexscenecontext)",
        "[Execution timeline](#execution-timeline)",
        "[Injection pass summary](#injection-pass-summary)"
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ProjectRootInstaller"
    },
    {
      "type": "paragraph",
      "text": "`ProjectRootInstaller` is an abstract `MonoBehaviour` that implements `IInstaller` and serves as the mandatory entry point for the Zenjex layer. It runs at `[DefaultExecutionOrder(-280)]`."
    },
    {
      "type": "paragraph",
      "text": "Three abstract methods need to be implemented:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "InstallBindings(ContainerBuilder)   - synchronous, fills the global container\nInstallGameInstanceRoutine()        - optional coroutine, runs after the container is built\nLaunchGame()                        - called after IInitializable services are initialized"
    },
    {
      "type": "paragraph",
      "text": "Internal `Awake` flow:"
    },
    {
      "type": "diagram",
      "kind": "flow",
      "title": "ProjectRootInstaller.Awake()",
      "steps": [
        {
          "label": "new ContainerBuilder()"
        },
        {
          "label": "InstallBindings(builder)",
          "note": "user code"
        },
        {
          "label": "RootContainer = builder.Build()"
        },
        {
          "label": "OnContainerReady?.Invoke()",
          "note": "ZenjexRunner Pass 1"
        },
        {
          "label": "StartCoroutine(LateInitRoutine)",
          "children": [
            {
              "label": "InstallGameInstanceRoutine()",
              "note": "optional async work"
            },
            {
              "label": "CallInitializables(RootContainer)",
              "note": "IInitializable.Initialize()"
            },
            {
              "label": "LaunchGame()",
              "note": "user code"
            },
            {
              "label": "OnGameLaunched?.Invoke()",
              "note": "ZenjexRunner Pass 2"
            }
          ]
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "`RootContainer` is stored as a static property on `ProjectRootInstaller`. There is a guard that prevents re-initialization if `Awake()` gets called again - this can happen when Reload Domain is disabled."
    },
    {
      "type": "paragraph",
      "text": "`OnContainerReady` and `OnGameLaunched` are the two synchronization points that `ZenjexRunner` uses to orchestrate injection passes."
    },
    {
      "type": "paragraph",
      "text": "In practice, bindings are usually split across multiple child `MonoInstaller` components:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class AppInstaller : ProjectRootInstaller\n{\n    [SerializeField] private InfrastructureInstaller _infra;\n    [SerializeField] private GameplayInstaller       _gameplay;\n\n    public override void InstallBindings(ContainerBuilder builder)\n    {\n        _infra.InstallBindings(builder);\n        _gameplay.InstallBindings(builder);\n    }\n\n    public override IEnumerator InstallGameInstanceRoutine()\n    {\n        yield return _infra.LoadAddressablesRoutine();\n    }\n\n    public override void LaunchGame() { /* GSM will start via IInitializable */ }\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "RootContext"
    },
    {
      "type": "paragraph",
      "text": "`RootContext` is a thin static wrapper over `ProjectRootInstaller.RootContainer`. It exists so that user code does not need to reference `ProjectRootInstaller` directly."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public static class RootContext\n{\n    public static Container Runtime => ProjectRootInstaller.RootContainer;\n    public static bool HasInstance   => ProjectRootInstaller.RootContainer != null;\n    public static T Resolve<T>()     { ... }\n    public static object Resolve(Type type) { ... }\n}"
    },
    {
      "type": "paragraph",
      "text": "`Runtime` gives direct access to the container, which makes post-initialization binding possible via `RootContext.Runtime.Bind<T>()` - an extension method on `Container` added by `ContainerZenjectExtensions`. Both `Resolve` overloads throw `InvalidOperationException` if the container has not been built yet."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "BindingBuilder"
    },
    {
      "type": "paragraph",
      "text": "`BindingBuilder<T>` is the backbone of Zenjex's Zenject-like fluent API. You get one by calling `builder.Bind<T>()` or `builder.BindInstance<T>(instance)` - extension methods on `ContainerBuilder` defined in `ReflexZenjectExtensions`."
    },
    {
      "type": "paragraph",
      "text": "The general pattern is: chain source modifiers, contract modifiers, and argument modifiers in any order, then finish with a lifetime method."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Source modifiers"
    },
    {
      "type": "table",
      "head": [
        "Method",
        "Description"
      ],
      "rows": [
        [
          "`.To<TConcrete>()`",
          "Bind interface/base T to a concrete implementation"
        ],
        [
          "`.FromInstance(instance)`",
          "Wrap a pre-existing instance; always registers as Singleton"
        ],
        [
          "`.FromComponentInNewPrefab(prefab)`",
          "Instantiate a prefab at resolve-time and extract the component; always Singleton + Lazy"
        ],
        [
          "`.FromComponentInHierarchy()`",
          "Find an existing component in the loaded scene; always Singleton + Lazy"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "`FromComponentInNewPrefab` has additional hierarchy modifiers: `.WithGameObjectName(name)` renames the root GameObject, `.UnderTransformGroup(groupName)` places the instance under a named scene-root group (created automatically if absent), and `.UnderTransform(parent)` places it under a specific `Transform`."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Argument injection"
    },
    {
      "type": "paragraph",
      "text": "`.WithArguments(params object[] args)` lets you pass explicit constructor arguments that bypass the container. Each value is matched to a constructor parameter **by type** (first match wins); parameters with no match are resolved from the container normally. This mirrors Zenject's `WithArguments`:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "builder.Bind<IInputService>()\n       .To<InputService>()\n       .WithArguments(playerInput, cinemachineInputProvider)\n       .AsSingle();\n\n// InputService(PlayerInput pi, CinemachineInputProvider cip, ILoggingService log)\n// pi, cip <- WithArguments  |  log <- container"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Contract modifiers"
    },
    {
      "type": "table",
      "head": [
        "Method",
        "Description"
      ],
      "rows": [
        [
          "`.BindInterfaces()`",
          "Register under all interfaces of the concrete type"
        ],
        [
          "`.BindInterfacesAndSelf()`",
          "Register under all interfaces and the concrete type itself"
        ]
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Sub-container support"
    },
    {
      "type": "paragraph",
      "text": "`.CopyIntoDirectSubContainers()` switches the binding to **Scoped** lifetime, so a fresh instance is created inside each `SceneInstaller` sub-container. That instance gets both global (RootContainer) and scene-local (SceneInstaller) dependencies in its constructor:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Global installer:\nbuilder.Bind<LevelProgressServiceResolver>()\n       .BindInterfacesAndSelf()\n       .CopyIntoDirectSubContainers()\n       .NonLazy();"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Lifetime terminators"
    },
    {
      "type": "table",
      "head": [
        "Method",
        "Zenject equivalent",
        "Behaviour"
      ],
      "rows": [
        [
          "`.AsSingle()`",
          "`.AsSingle()`",
          "Lazy singleton"
        ],
        [
          "`.AsSingleton()`",
          "-",
          "Alias for `AsSingle()`"
        ],
        [
          "`.NonLazy()`",
          "`.AsSingle().NonLazy()`",
          "Eager singleton - constructed immediately when the container is built"
        ],
        [
          "`.AsEagerSingleton()`",
          "-",
          "Alias for `NonLazy()`"
        ],
        [
          "`.AsTransient()`",
          "`.AsTransient()`",
          "New instance on every resolve"
        ],
        [
          "`.AsScoped()`",
          "-",
          "One instance per sub-container (SceneInstaller)"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "`Transient + Eager` is explicitly forbidden - a committed builder throws on this combination. Calling `Commit()` twice also throws."
    },
    {
      "type": "paragraph",
      "text": "When `.CopyIntoDirectSubContainers()` is set, `AsSingle()` and `NonLazy()` automatically promote the lifetime to `Scoped`."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ContainerBindingBuilder"
    },
    {
      "type": "paragraph",
      "text": "`ContainerBindingBuilder<T>` is used for **post-initialization binding** on a live `Container` via `RootContext.Runtime.Bind<T>()` (extension method from `ContainerZenjectExtensions`). It only supports registering pre-existing instances - registering new types into an already-built container is not possible."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "RootContext.Runtime\n    .Bind<ISomeService>()\n    .FromInstance(myService)\n    .BindInterfacesAndSelf()\n    .AsSingle();"
    },
    {
      "type": "paragraph",
      "text": "Calling `AsSingle()` without a prior `FromInstance()` throws `InvalidOperationException`."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "IInitializable"
    },
    {
      "type": "paragraph",
      "text": "`IInitializable` is a drop-in replacement for Zenject's `IInitializable`. Any service that implements it will receive a `Initialize()` call after the container is fully built and all dependencies are injected, but before `LaunchGame()` runs."
    },
    {
      "type": "paragraph",
      "text": "Execution order:"
    },
    {
      "type": "list",
      "items": [
        "`Container.Build()` - all bindings registered",
        "`OnContainerReady` - field/property injection pass",
        "`InstallGameInstanceRoutine()` - async setup (Addressables, etc.)",
        "**`IInitializable.Initialize()`** <- you are here",
        "`LaunchGame()` - user entry-point",
        "`OnGameLaunched` - late injection pass"
      ]
    },
    {
      "type": "paragraph",
      "text": "For the service to be discovered, its binding **must** expose `IInitializable` as a contract - use `.BindInterfaces()` or `.BindInterfacesAndSelf()` in the installer. `ProjectRootInstaller` calls `Initialize()` inside a try/catch and logs errors without interrupting the boot sequence. `SceneInstaller` does the same for scene-scoped services."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexInjector"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexInjector` is the core injection engine. It resolves all `[Zenjex]`-marked members on a target object from `RootContext`. It is called automatically by both `ZenjexRunner` and `ZenjexBehaviour`."
    },
    {
      "type": "paragraph",
      "text": "The injector has its own reflection cache (`Dictionary<Type, TypeZenjexInfo>`) that stores three arrays per type: `FieldInfo[]`, `PropertyInfo[]`, `MethodInfo[]`. The cache is built lazily and walks the full inheritance chain (`t = t.BaseType`) to pick up members declared in base classes - this is important for `ZenjexBehaviour` subclasses."
    },
    {
      "type": "paragraph",
      "text": "`HasZenjexMembers(Type)` is a fast pre-check that `ZenjexRunner` uses to skip `MonoBehaviour` types with no `[Zenjex]` members, without building a full `TypeZenjexInfo` entry for them."
    },
    {
      "type": "paragraph",
      "text": "Injection errors per member are caught individually and logged via `Debug.LogError`, so one failed resolve does not block the rest of the injection pass."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexRunner"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexRunner` is the scene-wide injection orchestrator. It is a static class that initializes via `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` and subscribes to the two `ProjectRootInstaller` events and `SceneManager.sceneLoaded`."
    },
    {
      "type": "paragraph",
      "text": "Internal state:"
    },
    {
      "type": "list",
      "items": [
        "`_injected: HashSet<int>` - instance IDs of already-injected objects, guards against double injection",
        "`_launched: bool` - whether `OnGameLaunched` has fired (exposed publicly as `IsReady`)",
        "`InjectedRecords: List<InjectedRecord>` - a log of every injection, consumed by the debugger window",
        "`OnStateChanged: event Action` - fires after each injection pass or after a manual inject call"
      ]
    },
    {
      "type": "paragraph",
      "text": "**Pass 1 - `OnContainerReady`** (fires synchronously inside `ProjectRootInstaller.Awake()` at order `-280`): Walks all loaded scenes via `SceneManager.GetSceneAt(i)`, collects all `MonoBehaviour` components including inactive ones, checks `HasZenjexMembers`, then calls `ZenjexInjector.Inject`. Since this runs at order `-280`, every `Awake()` that follows will already see populated fields."
    },
    {
      "type": "paragraph",
      "text": "**Pass 2 - `OnGameLaunched`** (fires after `InstallGameInstanceRoutine()` and `IInitializable.Initialize()` complete): Repeats the same scene walk. This catches objects whose dependencies were registered during `InstallGameInstanceRoutine` and could not be resolved in Pass 1. Sets `_launched = true`."
    },
    {
      "type": "paragraph",
      "text": "**Pass 3 - `SceneManager.sceneLoaded`** (fires for each additively loaded scene after launch): Same walk, targeting late-arriving scenes. Any `MonoBehaviour` injected here already had its `Awake()` run before injection - the runner emits a `ZNX-LATE` warning. Use `ZenjexBehaviour` to avoid this."
    },
    {
      "type": "paragraph",
      "text": "**Pass 4 - `InjectGameObject(GameObject go)`** (manual, runtime): Call right after `Instantiate()` for dynamically spawned objects. If called before `LaunchGame()`, the call is skipped and a warning is emitted."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "var enemy = Instantiate(enemyPrefab);\nZenjexRunner.InjectGameObject(enemy);"
    },
    {
      "type": "paragraph",
      "text": "**Double-injection protection**: `ZenjexBehaviour.Awake()` calls `ZenjexRunner.MarkInjected(this)` after injecting itself. The runner skips any instance ID already in `_injected`, so there is no chance of a double resolve between the base class and the runner passes."
    },
    {
      "type": "paragraph",
      "text": "Every injection is recorded in `InjectedRecords` as an `InjectedRecord` (type name, GameObject name, scene name, pass, late flag) for the debugger window."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexBehaviour"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexBehaviour` is an abstract `MonoBehaviour` running at `[DefaultExecutionOrder(-100)]`. It gives the strongest injection timing guarantee available: fields are injected inside its own `Awake()`, before any default-order `Awake()` runs. Unlike the runner passes, it also works on prefabs spawned via `Instantiate()`, since execution order applies to any active `MonoBehaviour` regardless of how it arrived in the scene."
    },
    {
      "type": "paragraph",
      "text": "If `RootContext.HasInstance` is false when `Awake()` runs (the container is not built yet), injection is skipped and a warning is logged; `OnAwake()` still fires."
    },
    {
      "type": "paragraph",
      "text": "Usage:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class MyController : ZenjexBehaviour\n{\n    [Zenjex] private IMyService _service;\n\n    protected override void OnAwake()\n    {\n        // _service is already injected here\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "`OnAwake()` is a `protected virtual` method. Subclasses no longer need to call `base.OnAwake()` - `ZenjexRunner.MarkInjected` is called directly in `ZenjexBehaviour.Awake()` before `OnAwake()` is invoked. Calling `base.OnAwake()` in a subclass is harmless (the base implementation is empty) but not required."
    },
    {
      "type": "paragraph",
      "text": "`SceneInstaller` also extends `ZenjexBehaviour` (at order `-200`) so it can receive global `[Zenjex]` injections before it builds the scene container."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "SceneInstaller"
    },
    {
      "type": "paragraph",
      "text": "`SceneInstaller` is an abstract `MonoBehaviour` that extends `ZenjexBehaviour` and runs at `[DefaultExecutionOrder(-200)]`. It is the Zenjex equivalent of Zenject's `SceneContext`. Place one per gameplay scene on any GameObject."
    },
    {
      "type": "paragraph",
      "text": "It creates a **child container** that inherits all global bindings from `ProjectRootInstaller.RootContainer` and adds scene-local bindings on top:"
    },
    {
      "type": "diagram",
      "kind": "hierarchy",
      "nodes": [
        {
          "label": "RootContainer",
          "sub": "global · ProjectRootInstaller",
          "children": [
            {
              "label": "SceneContainer",
              "sub": "per-scene · SceneInstaller"
            }
          ]
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "Lifecycle inside `OnAwake()` (runs at order `-200`, after `ProjectRootInstaller` at `-280`):"
    },
    {
      "type": "diagram",
      "kind": "flow",
      "title": "SceneInstaller.OnAwake()",
      "steps": [
        {
          "label": "RootContext.Runtime.Scope(InstallBindings)",
          "note": "creates child container"
        },
        {
          "label": "ZenjexSceneContext.Register(scene, container)"
        },
        {
          "label": "CallInitializables(SceneContainer)",
          "note": "IInitializable on scene-local services"
        },
        {
          "label": "OnSceneContainerReady?.Invoke(container)"
        },
        {
          "label": "OnInstalled()",
          "note": "optional override"
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "The child container is disposed in `OnDestroy()` when the scene unloads, which triggers `IDisposable.Dispose()` on all scoped instances."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class GameplaySceneInstaller : SceneInstaller\n{\n    [SerializeField] private LevelProgressWatcher levelProgressWatcher;\n\n    public override void InstallBindings(ContainerBuilder builder)\n    {\n        builder.BindInstance(levelProgressWatcher).AsSingle();\n\n        builder.Bind<LevelProgressServiceResolver>()\n               .BindInterfacesAndSelf()\n               .CopyIntoDirectSubContainers()\n               .NonLazy();\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "Any global binding marked with `.CopyIntoDirectSubContainers()` (Scoped lifetime) will be freshly instantiated inside this scene's container and can receive both global and scene-local dependencies."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexSceneContext"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexSceneContext` is a static registry of scene-scoped containers. `SceneInstaller` populates and cleans it up automatically."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Resolve from the most recently loaded scene container:\nZenjexSceneContext.Resolve<LevelProgressWatcher>();\n\n// Get the container itself for multiple lookups:\nvar container = ZenjexSceneContext.GetActive();\n\n// Get a container for a specific scene:\nvar container = ZenjexSceneContext.Get(scene);\n\n// Guard before resolving:\nif (ZenjexSceneContext.HasActiveScene) { ... }"
    },
    {
      "type": "paragraph",
      "text": "Internally, containers are stored in a `Dictionary<int, Container>` keyed by `Scene.handle`. `GetActive()` returns the container from the most recently loaded `SceneInstaller`; `Get(scene)` lets you target a specific scene directly."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Execution timeline"
    },
    {
      "type": "diagram",
      "kind": "timeline",
      "groups": [
        {
          "label": "Application start",
          "items": [
            {
              "label": "RuntimeInitializeOnLoadMethod: AfterAssembliesLoaded",
              "sub": "UnityInjector.Initialize()"
            },
            {
              "label": "RuntimeInitializeOnLoadMethod: BeforeSceneLoad",
              "sub": "ZenjexRunner.Initialize()"
            }
          ]
        },
        {
          "label": "Scene loads",
          "items": [
            {
              "label": "ContainerScope.Awake()",
              "order": "-1,000,000,000",
              "sub": "UnityInjector creates SceneContainer"
            },
            {
              "label": "ProjectRootInstaller.Awake()",
              "order": "-280",
              "sub": "Build container → OnContainerReady → Pass 1"
            },
            {
              "label": "SceneInstaller.OnAwake()",
              "order": "-200",
              "sub": "Create scene container → scene IInitializables"
            },
            {
              "label": "ZenjexBehaviour.Awake()",
              "order": "-100",
              "sub": "Inject(this) → OnAwake()"
            },
            {
              "label": "MonoBehaviour.Awake()",
              "order": "0",
              "sub": "[Zenjex] fields populated from Pass 1"
            }
          ]
        },
        {
          "label": "After yield (coroutine)",
          "items": [
            {
              "label": "InstallGameInstanceRoutine()",
              "sub": "optional async initialization"
            },
            {
              "label": "CallInitializables(RootContainer)",
              "sub": "IInitializable.Initialize()"
            },
            {
              "label": "LaunchGame()",
              "sub": "game entry point"
            },
            {
              "label": "OnGameLaunched.Invoke()",
              "sub": "ZenjexRunner Pass 2"
            }
          ]
        }
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Injection pass summary"
    },
    {
      "type": "table",
      "head": [
        "Pass",
        "Trigger",
        "Fires before `Awake()`?",
        "ZNX-LATE warning?"
      ],
      "rows": [
        [
          "Pass 1",
          "`OnContainerReady` (inside `ProjectRootInstaller.Awake` at -280)",
          "Yes",
          "No"
        ],
        [
          "Pass 2",
          "`OnGameLaunched` (after coroutine + `IInitializable`)",
          "No",
          "No"
        ],
        [
          "Pass 3",
          "`SceneManager.sceneLoaded` (additive scenes)",
          "No",
          "**Yes**"
        ],
        [
          "Pass 4",
          "`ZenjexRunner.InjectGameObject()` (manual, runtime)",
          "No",
          "No"
        ],
        [
          "`ZenjexBehaviour`",
          "Own `Awake()` at order -100",
          "Yes",
          "No"
        ],
        [
          "`SceneInstaller`",
          "Own `OnAwake()` at order -200 (ZenjexBehaviour subclass)",
          "Yes",
          "No"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "← [Reflex Internals](Reflex-Internals) | [Home](Home)"
    }
  ],
  "ru": [
    {
      "type": "heading",
      "level": 1,
      "text": "Zenjex: внутреннее устройство"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Содержание"
    },
    {
      "type": "list",
      "items": [
        "[ProjectRootInstaller](#projectrootinstaller)",
        "[RootContext](#rootcontext)",
        "[BindingBuilder](#bindingbuilder)",
        "[ContainerBindingBuilder](#containerbindingbuilder)",
        "[IInitializable](#iinitializable)",
        "[ZenjexInjector](#zenjexinjector)",
        "[ZenjexRunner](#zenjexrunner)",
        "[ZenjexBehaviour](#zenjexbehaviour)",
        "[SceneInstaller](#sceneinstaller)",
        "[ZenjexSceneContext](#zenjexscenecontext)",
        "[Хронология выполнения](#хронология-выполнения)",
        "[Сводная таблица проходов инъекции](#сводная-таблица-проходов-инъекции)"
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ProjectRootInstaller"
    },
    {
      "type": "paragraph",
      "text": "`ProjectRootInstaller` - абстрактный `MonoBehaviour`, реализующий `IInstaller`. Это обязательная точка входа для всего слоя Zenjex. Запускается при `[DefaultExecutionOrder(-280)]`."
    },
    {
      "type": "paragraph",
      "text": "Нужно реализовать три абстрактных метода:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "InstallBindings(ContainerBuilder)   - синхронный, заполняет глобальный контейнер\nInstallGameInstanceRoutine()        - опциональная корутина, запускается после сборки контейнера\nLaunchGame()                        - вызывается после инициализации IInitializable-сервисов"
    },
    {
      "type": "paragraph",
      "text": "Порядок выполнения внутри `Awake`:"
    },
    {
      "type": "diagram",
      "kind": "flow",
      "title": "ProjectRootInstaller.Awake()",
      "steps": [
        {
          "label": "new ContainerBuilder()"
        },
        {
          "label": "InstallBindings(builder)",
          "note": "пользовательский код"
        },
        {
          "label": "RootContainer = builder.Build()"
        },
        {
          "label": "OnContainerReady?.Invoke()",
          "note": "Pass 1 ZenjexRunner"
        },
        {
          "label": "StartCoroutine(LateInitRoutine)",
          "children": [
            {
              "label": "InstallGameInstanceRoutine()",
              "note": "асинхронная работа"
            },
            {
              "label": "CallInitializables(RootContainer)",
              "note": "IInitializable.Initialize()"
            },
            {
              "label": "LaunchGame()",
              "note": "пользовательский код"
            },
            {
              "label": "OnGameLaunched?.Invoke()",
              "note": "Pass 2 ZenjexRunner"
            }
          ]
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "`RootContainer` хранится как статическое свойство `ProjectRootInstaller`. Повторный вызов `Awake()` защищён guard-проверкой - актуально при отключённом Reload Domain."
    },
    {
      "type": "paragraph",
      "text": "`OnContainerReady` и `OnGameLaunched` - две точки синхронизации, через которые `ZenjexRunner` управляет проходами инъекции."
    },
    {
      "type": "paragraph",
      "text": "На практике биндинги обычно разносятся по нескольким дочерним `MonoInstaller`-компонентам:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class AppInstaller : ProjectRootInstaller\n{\n    [SerializeField] private InfrastructureInstaller _infra;\n    [SerializeField] private GameplayInstaller       _gameplay;\n\n    public override void InstallBindings(ContainerBuilder builder)\n    {\n        _infra.InstallBindings(builder);\n        _gameplay.InstallBindings(builder);\n    }\n\n    public override IEnumerator InstallGameInstanceRoutine()\n    {\n        yield return _infra.LoadAddressablesRoutine();\n    }\n\n    public override void LaunchGame() { /* GSM запустится через IInitializable */ }\n}"
    },
    {
      "type": "heading",
      "level": 2,
      "text": "RootContext"
    },
    {
      "type": "paragraph",
      "text": "`RootContext` - тонкая статическая обёртка над `ProjectRootInstaller.RootContainer`. Нужна для того, чтобы пользовательский код не обращался к `ProjectRootInstaller` напрямую."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public static class RootContext\n{\n    public static Container Runtime => ProjectRootInstaller.RootContainer;\n    public static bool HasInstance   => ProjectRootInstaller.RootContainer != null;\n    public static T Resolve<T>()     { ... }\n    public static object Resolve(Type type) { ... }\n}"
    },
    {
      "type": "paragraph",
      "text": "`Runtime` открывает прямой доступ к контейнеру, что позволяет добавлять биндинги после инициализации через `RootContext.Runtime.Bind<T>()` - это extension-метод на `Container`, добавленный в `ContainerZenjectExtensions`. Оба перегруженных `Resolve` бросают `InvalidOperationException`, если контейнер ещё не был собран."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "BindingBuilder"
    },
    {
      "type": "paragraph",
      "text": "`BindingBuilder<T>` - основа fluent-API Zenjex, воспроизводящего стиль Zenject. Получить его можно вызовом `builder.Bind<T>()` или `builder.BindInstance<T>(instance)` - это extension-методы на `ContainerBuilder`, объявленные в `ReflexZenjectExtensions`."
    },
    {
      "type": "paragraph",
      "text": "Общий паттерн: сначала модификаторы источника, контрактов и аргументов в любом порядке, затем терминатор лайфтайма."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Модификаторы источника"
    },
    {
      "type": "table",
      "head": [
        "Метод",
        "Описание"
      ],
      "rows": [
        [
          "`.To<TConcrete>()`",
          "Привязать интерфейс/базовый тип T к конкретной реализации"
        ],
        [
          "`.FromInstance(instance)`",
          "Обернуть уже существующий экземпляр; всегда регистрируется как Singleton"
        ],
        [
          "`.FromComponentInNewPrefab(prefab)`",
          "Заинстанцировать префаб при резолве и извлечь компонент; всегда Singleton + Lazy"
        ],
        [
          "`.FromComponentInHierarchy()`",
          "Найти существующий компонент в иерархии загруженной сцены; всегда Singleton + Lazy"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "У `FromComponentInNewPrefab` есть дополнительные модификаторы иерархии: `.WithGameObjectName(name)` переименовывает корневой GameObject, `.UnderTransformGroup(groupName)` помещает объект под именованную группу в корне сцены (создаётся автоматически, если отсутствует), `.UnderTransform(parent)` помещает его под конкретный `Transform`."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Инъекция аргументов"
    },
    {
      "type": "paragraph",
      "text": "`.WithArguments(params object[] args)` позволяет передать в конструктор явные аргументы, минуя контейнер. Каждое значение сопоставляется с параметром конструктора **по типу** (побеждает первое совпадение); параметры без совпадения резолвятся из контейнера в штатном режиме. Поведение идентично `WithArguments` в Zenject:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "builder.Bind<IInputService>()\n       .To<InputService>()\n       .WithArguments(playerInput, cinemachineInputProvider)\n       .AsSingle();\n\n// InputService(PlayerInput pi, CinemachineInputProvider cip, ILoggingService log)\n// pi, cip <- WithArguments  |  log <- контейнер"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Модификаторы контрактов"
    },
    {
      "type": "table",
      "head": [
        "Метод",
        "Описание"
      ],
      "rows": [
        [
          "`.BindInterfaces()`",
          "Зарегистрировать под всеми интерфейсами конкретного типа"
        ],
        [
          "`.BindInterfacesAndSelf()`",
          "Зарегистрировать под всеми интерфейсами и самим конкретным типом"
        ]
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Поддержка саб-контейнеров"
    },
    {
      "type": "paragraph",
      "text": "`.CopyIntoDirectSubContainers()` переключает биндинг на **Scoped** лайфтайм, так что внутри каждого `SceneInstaller`-саб-контейнера создаётся свежий экземпляр. Этот экземпляр получает в конструктор как глобальные зависимости (RootContainer), так и локальные для сцены (SceneInstaller):"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Глобальный инсталлер:\nbuilder.Bind<LevelProgressServiceResolver>()\n       .BindInterfacesAndSelf()\n       .CopyIntoDirectSubContainers()\n       .NonLazy();"
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Терминаторы лайфтайма"
    },
    {
      "type": "table",
      "head": [
        "Метод",
        "Аналог в Zenject",
        "Поведение"
      ],
      "rows": [
        [
          "`.AsSingle()`",
          "`.AsSingle()`",
          "Lazy singleton"
        ],
        [
          "`.AsSingleton()`",
          "-",
          "Псевдоним для `AsSingle()`"
        ],
        [
          "`.NonLazy()`",
          "`.AsSingle().NonLazy()`",
          "Eager singleton - создаётся сразу при сборке контейнера"
        ],
        [
          "`.AsEagerSingleton()`",
          "-",
          "Псевдоним для `NonLazy()`"
        ],
        [
          "`.AsTransient()`",
          "`.AsTransient()`",
          "Новый экземпляр при каждом резолве"
        ],
        [
          "`.AsScoped()`",
          "-",
          "Один экземпляр на саб-контейнер (SceneInstaller)"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "`Transient + Eager` явно запрещён - закоммиченный билдер бросит исключение на такой комбинации. Повторный вызов `Commit()` тоже бросает."
    },
    {
      "type": "paragraph",
      "text": "Если активен `.CopyIntoDirectSubContainers()`, вызовы `AsSingle()` и `NonLazy()` автоматически повышают лайфтайм до `Scoped`."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ContainerBindingBuilder"
    },
    {
      "type": "paragraph",
      "text": "`ContainerBindingBuilder<T>` используется для **пост-инициализационных биндингов** на живом `Container` через `RootContext.Runtime.Bind<T>()` (extension-метод из `ContainerZenjectExtensions`). Поддерживается только регистрация уже существующих экземпляров - зарегистрировать новый тип в уже собранном контейнере нельзя."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "RootContext.Runtime\n    .Bind<ISomeService>()\n    .FromInstance(myService)\n    .BindInterfacesAndSelf()\n    .AsSingle();"
    },
    {
      "type": "paragraph",
      "text": "Вызов `AsSingle()` без предшествующего `FromInstance()` бросает `InvalidOperationException`."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "IInitializable"
    },
    {
      "type": "paragraph",
      "text": "`IInitializable` - аналог одноимённого интерфейса из Zenject. Любой сервис, реализующий его, получит вызов `Initialize()` после того, как контейнер полностью собран и все зависимости внедрены, но до вызова `LaunchGame()`."
    },
    {
      "type": "paragraph",
      "text": "Порядок выполнения:"
    },
    {
      "type": "list",
      "items": [
        "`Container.Build()` - все биндинги зарегистрированы",
        "`OnContainerReady` - проход инъекции полей и свойств",
        "`InstallGameInstanceRoutine()` - асинхронная настройка (Addressables и пр.)",
        "**`IInitializable.Initialize()`** <- вы здесь",
        "`LaunchGame()` - точка входа пользователя",
        "`OnGameLaunched` - поздний проход инъекции"
      ]
    },
    {
      "type": "paragraph",
      "text": "Чтобы сервис был обнаружен, его биндинг **обязательно** должен выставлять `IInitializable` как контракт - используйте `.BindInterfaces()` или `.BindInterfacesAndSelf()` в инсталлере. `ProjectRootInstaller` вызывает `Initialize()` внутри try/catch и логирует ошибки, не прерывая последовательность загрузки. `SceneInstaller` делает то же самое для scene-scoped сервисов."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexInjector"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexInjector` - ядро механизма инъекции. Резолвит все члены, помеченные `[Zenjex]`, из `RootContext`. Вызывается автоматически как `ZenjexRunner`, так и `ZenjexBehaviour`."
    },
    {
      "type": "paragraph",
      "text": "Инжектор ведёт собственный кэш рефлексии (`Dictionary<Type, TypeZenjexInfo>`), в котором хранятся три массива на тип: `FieldInfo[]`, `PropertyInfo[]`, `MethodInfo[]`. Кэш строится лениво и проходит вверх по цепочке наследования (`t = t.BaseType`), подхватывая члены, объявленные в базовых классах - это важно для подклассов `ZenjexBehaviour`."
    },
    {
      "type": "paragraph",
      "text": "`HasZenjexMembers(Type)` - быстрая предварительная проверка, которую `ZenjexRunner` использует, чтобы пропустить `MonoBehaviour`-типы без `[Zenjex]`-членов, не создавая для них полную запись `TypeZenjexInfo`."
    },
    {
      "type": "paragraph",
      "text": "Ошибки инъекции на каждом члене перехватываются по отдельности и логируются через `Debug.LogError`, так что один неудавшийся резолв не блокирует остальную часть прохода."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexRunner"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexRunner` - оркестратор инъекции на уровне всей сцены. Статический класс, который инициализируется через `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` и подписывается на два события `ProjectRootInstaller` и `SceneManager.sceneLoaded`."
    },
    {
      "type": "paragraph",
      "text": "Внутреннее состояние:"
    },
    {
      "type": "list",
      "items": [
        "`_injected: HashSet<int>` - instance ID уже обработанных объектов, защита от двойной инъекции",
        "`_launched: bool` - был ли уже вызван `OnGameLaunched` (публично доступен как `IsReady`)",
        "`InjectedRecords: List<InjectedRecord>` - лог всех выполненных инъекций, используется окном отладчика",
        "`OnStateChanged: event Action` - срабатывает после каждого прохода инъекции или ручного вызова"
      ]
    },
    {
      "type": "paragraph",
      "text": "**Pass 1 - `OnContainerReady`** (срабатывает синхронно внутри `ProjectRootInstaller.Awake()` при порядке `-280`): Обходит все загруженные сцены через `SceneManager.GetSceneAt(i)`, собирает все `MonoBehaviour`-компоненты включая неактивные, проверяет `HasZenjexMembers` и вызывает `ZenjexInjector.Inject`. Поскольку это происходит при порядке `-280`, каждый последующий `Awake()` в сцене уже увидит заполненные поля."
    },
    {
      "type": "paragraph",
      "text": "**Pass 2 - `OnGameLaunched`** (срабатывает после завершения `InstallGameInstanceRoutine()` и `IInitializable.Initialize()`): Повторяет тот же обход сцены. Покрывает объекты, зависимости которых были зарегистрированы в `InstallGameInstanceRoutine` и потому не могли быть резолвнуты в Pass 1. Устанавливает `_launched = true`."
    },
    {
      "type": "paragraph",
      "text": "**Pass 3 - `SceneManager.sceneLoaded`** (срабатывает для каждой аддитивно загруженной сцены после запуска): Тот же обход, но для сцен, загруженных позже. Любой `MonoBehaviour`, инъецированный в этом проходе, уже выполнил свой `Awake()` до инъекции - раннер выдаёт предупреждение `ZNX-LATE`. Чтобы избежать этого, используйте `ZenjexBehaviour`."
    },
    {
      "type": "paragraph",
      "text": "**Pass 4 - `InjectGameObject(GameObject go)`** (ручной, runtime): Вызывается сразу после `Instantiate()` для динамически созданных объектов. Если вызвать до `LaunchGame()`, вызов будет проигнорирован и выдано предупреждение."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "var enemy = Instantiate(enemyPrefab);\nZenjexRunner.InjectGameObject(enemy);"
    },
    {
      "type": "paragraph",
      "text": "**Защита от двойной инъекции**: `ZenjexBehaviour.Awake()` вызывает `ZenjexRunner.MarkInjected(this)` после того, как сам выполнил инъекцию. Раннер пропускает instance ID, уже присутствующий в `_injected`, так что двойного резолва между базовым классом и проходами раннера не возникает."
    },
    {
      "type": "paragraph",
      "text": "Каждая инъекция записывается в `InjectedRecords` как `InjectedRecord` (имя типа, имя GameObject, имя сцены, номер прохода, флаг опоздания) для окна отладчика."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexBehaviour"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexBehaviour` - абстрактный `MonoBehaviour` с `[DefaultExecutionOrder(-100)]`. Даёт наиболее строгую гарантию по времени инъекции: поля заполняются в собственном `Awake()`, до запуска любого `Awake()` с порядком по умолчанию. В отличие от проходов раннера, это работает и для префабов, созданных через `Instantiate()`, - порядок выполнения применяется к любому активному `MonoBehaviour` вне зависимости от того, как он попал в сцену."
    },
    {
      "type": "paragraph",
      "text": "Если `RootContext.HasInstance` возвращает false в момент вызова `Awake()` (контейнер ещё не собран), инъекция пропускается и выдаётся предупреждение; `OnAwake()` при этом всё равно вызывается."
    },
    {
      "type": "paragraph",
      "text": "Пример использования:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class MyController : ZenjexBehaviour\n{\n    [Zenjex] private IMyService _service;\n\n    protected override void OnAwake()\n    {\n        // _service уже доступен здесь\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "`OnAwake()` - `protected virtual` метод. Подклассам больше не нужно вызывать `base.OnAwake()` - `ZenjexRunner.MarkInjected` вызывается напрямую в `ZenjexBehaviour.Awake()` до того, как выполняется `OnAwake()`. Вызов `base.OnAwake()` в подклассе безвреден (базовая реализация пустая), но не обязателен."
    },
    {
      "type": "paragraph",
      "text": "`SceneInstaller` тоже наследует `ZenjexBehaviour` (с порядком `-200`), чтобы получить глобальные `[Zenjex]`-инъекции до того, как начнёт строить контейнер сцены."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "SceneInstaller"
    },
    {
      "type": "paragraph",
      "text": "`SceneInstaller` - абстрактный `MonoBehaviour`, наследующий `ZenjexBehaviour`, с `[DefaultExecutionOrder(-200)]`. Аналог `SceneContext` из Zenject. Размещается в каждой игровой сцене на любом GameObject."
    },
    {
      "type": "paragraph",
      "text": "Создаёт **дочерний контейнер**, наследующий все глобальные биндинги из `ProjectRootInstaller.RootContainer`, и добавляет к ним локальные биндинги сцены:"
    },
    {
      "type": "diagram",
      "kind": "hierarchy",
      "nodes": [
        {
          "label": "RootContainer",
          "sub": "глобальный · ProjectRootInstaller",
          "children": [
            {
              "label": "SceneContainer",
              "sub": "для сцены · SceneInstaller"
            }
          ]
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "Жизненный цикл внутри `OnAwake()` (порядок `-200`, после `ProjectRootInstaller` с порядком `-280`):"
    },
    {
      "type": "diagram",
      "kind": "flow",
      "title": "SceneInstaller.OnAwake()",
      "steps": [
        {
          "label": "RootContext.Runtime.Scope(InstallBindings)",
          "note": "создаётся дочерний контейнер"
        },
        {
          "label": "ZenjexSceneContext.Register(scene, container)"
        },
        {
          "label": "CallInitializables(SceneContainer)",
          "note": "IInitializable для scene-local сервисов"
        },
        {
          "label": "OnSceneContainerReady?.Invoke(container)"
        },
        {
          "label": "OnInstalled()",
          "note": "опциональный override"
        }
      ]
    },
    {
      "type": "paragraph",
      "text": "Дочерний контейнер уничтожается в `OnDestroy()` при выгрузке сцены, что автоматически вызывает `IDisposable.Dispose()` на всех scoped-экземплярах."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "public class GameplaySceneInstaller : SceneInstaller\n{\n    [SerializeField] private LevelProgressWatcher levelProgressWatcher;\n\n    public override void InstallBindings(ContainerBuilder builder)\n    {\n        builder.BindInstance(levelProgressWatcher).AsSingle();\n\n        builder.Bind<LevelProgressServiceResolver>()\n               .BindInterfacesAndSelf()\n               .CopyIntoDirectSubContainers()\n               .NonLazy();\n    }\n}"
    },
    {
      "type": "paragraph",
      "text": "Любой глобальный биндинг с `.CopyIntoDirectSubContainers()` (Scoped лайфтайм) будет заново создан внутри контейнера этой сцены и сможет получить как глобальные, так и локальные зависимости."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "ZenjexSceneContext"
    },
    {
      "type": "paragraph",
      "text": "`ZenjexSceneContext` - статический реестр scene-scoped контейнеров. `SceneInstaller` заполняет и очищает его автоматически."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Резолвнуть из последнего загруженного контейнера сцены:\nZenjexSceneContext.Resolve<LevelProgressWatcher>();\n\n// Получить сам контейнер для нескольких резолвов:\nvar container = ZenjexSceneContext.GetActive();\n\n// Получить контейнер конкретной сцены:\nvar container = ZenjexSceneContext.Get(scene);\n\n// Проверить наличие активного контейнера:\nif (ZenjexSceneContext.HasActiveScene) { ... }"
    },
    {
      "type": "paragraph",
      "text": "Контейнеры хранятся в `Dictionary<int, Container>` с ключом `Scene.handle`. `GetActive()` возвращает контейнер, зарегистрированный последним загруженным `SceneInstaller`; `Get(scene)` позволяет адресовать конкретную сцену напрямую."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Хронология выполнения"
    },
    {
      "type": "diagram",
      "kind": "timeline",
      "groups": [
        {
          "label": "Запуск приложения",
          "items": [
            {
              "label": "RuntimeInitializeOnLoadMethod: AfterAssembliesLoaded",
              "sub": "UnityInjector.Initialize()"
            },
            {
              "label": "RuntimeInitializeOnLoadMethod: BeforeSceneLoad",
              "sub": "ZenjexRunner.Initialize()"
            }
          ]
        },
        {
          "label": "Загрузка сцены",
          "items": [
            {
              "label": "ContainerScope.Awake()",
              "order": "-1 000 000 000",
              "sub": "UnityInjector создаёт SceneContainer"
            },
            {
              "label": "ProjectRootInstaller.Awake()",
              "order": "-280",
              "sub": "Сборка контейнера → OnContainerReady → Pass 1"
            },
            {
              "label": "SceneInstaller.OnAwake()",
              "order": "-200",
              "sub": "Создание контейнера сцены → IInitializable"
            },
            {
              "label": "ZenjexBehaviour.Awake()",
              "order": "-100",
              "sub": "Inject(this) → OnAwake()"
            },
            {
              "label": "MonoBehaviour.Awake()",
              "order": "0",
              "sub": "[Zenjex]-поля уже заполнены из Pass 1"
            }
          ]
        },
        {
          "label": "После yield (корутина)",
          "items": [
            {
              "label": "InstallGameInstanceRoutine()",
              "sub": "асинхронная инициализация"
            },
            {
              "label": "CallInitializables(RootContainer)",
              "sub": "IInitializable.Initialize()"
            },
            {
              "label": "LaunchGame()",
              "sub": "точка входа в игру"
            },
            {
              "label": "OnGameLaunched.Invoke()",
              "sub": "ZenjexRunner Pass 2"
            }
          ]
        }
      ]
    },
    {
      "type": "heading",
      "level": 2,
      "text": "Сводная таблица проходов инъекции"
    },
    {
      "type": "table",
      "head": [
        "Проход",
        "Триггер",
        "До `Awake()`?",
        "Предупреждение ZNX-LATE?"
      ],
      "rows": [
        [
          "Pass 1",
          "`OnContainerReady` (внутри `ProjectRootInstaller.Awake` при порядке -280)",
          "Да",
          "Нет"
        ],
        [
          "Pass 2",
          "`OnGameLaunched` (после корутины + `IInitializable`)",
          "Нет",
          "Нет"
        ],
        [
          "Pass 3",
          "`SceneManager.sceneLoaded` (аддитивные сцены)",
          "Нет",
          "**Да**"
        ],
        [
          "Pass 4",
          "`ZenjexRunner.InjectGameObject()` (вручную, runtime)",
          "Нет",
          "Нет"
        ],
        [
          "`ZenjexBehaviour`",
          "Собственный `Awake()` при порядке -100",
          "Да",
          "Нет"
        ],
        [
          "`SceneInstaller`",
          "Собственный `OnAwake()` при порядке -200 (подкласс ZenjexBehaviour)",
          "Да",
          "Нет"
        ]
      ]
    },
    {
      "type": "paragraph",
      "text": "← [Reflex: внутреннее устройство](Reflex-Internals.ru) | [Главная](Home.ru)"
    }
  ]
}