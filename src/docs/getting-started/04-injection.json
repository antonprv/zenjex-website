{
  "id": "injection",
  "titleRu": "Инъекции",
  "titleEn": "Injection",
  "en": [
    {
      "type": "heading",
      "level": 3,
      "text": "Lifecycle"
    },
    {
      "type": "paragraph",
      "text": "`ProjectRootInstaller` guarantees the following execution order every time the game starts:"
    },
    {
      "type": "list",
      "items": [
        "**`InstallBindings(builder)`** - register all global services. Runs synchronously inside `Awake()` at execution order `-280`.",
        "**`OnContainerReady`** - first `[Zenjex]` injection pass across all loaded scenes. Because this fires inside `Awake()` at order `-280`, all subsequent `Awake()` calls in the scene already see injected fields.",
        "**`InstallGameInstanceRoutine()`** - async setup: load Addressables, register runtime-only bindings, etc.",
        "**`IInitializable.Initialize()`** - called automatically on every service that implements `IInitializable` and is registered under that interface. Fires after step 3, before `LaunchGame()`.",
        "**`LaunchGame()`** - your game's entry point.",
        "**`OnGameLaunched`** - second `[Zenjex]` injection pass, covers objects that depend on bindings added in step 3."
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "IInitializable"
    },
    {
      "type": "paragraph",
      "text": "Implement `IInitializable` on any service to receive a guaranteed `Initialize()` call after the container is fully built and all dependencies are injected - but before `LaunchGame()` runs. This is a drop-in replacement for Zenject's `IInitializable`."
    },
    {
      "type": "paragraph",
      "text": "To be discovered, the service **must** expose `IInitializable` as a contract - use `BindInterfaces()` or `BindInterfacesAndSelf()` in the installer."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Lifecycle;\n\npublic class GameStateMachine : IInitializable\n{\n    private readonly StateFactory _stateFactory;\n\n    public GameStateMachine(StateFactory stateFactory) =>\n        _stateFactory = stateFactory;\n\n    public void Initialize() => Enter<BootstrapState>();\n}\n\n// In installer:\nbuilder.Bind<GameStateMachine>()\n       .BindInterfacesAndSelf()\n       .AsEagerSingleton();"
    },
    {
      "type": "paragraph",
      "text": "`IInitializable` works in both the global container (`ProjectRootInstaller`) and scene-scoped containers (`SceneInstaller`). Scene-local initializables are called right after the scene container is built, before `OnInstalled()`."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Post-initialization binding"
    },
    {
      "type": "paragraph",
      "text": "Sometimes you need to register something into the container after it's already been built - for example, a runtime-loaded config or a service created during `InstallGameInstanceRoutine`. Use `RootContext.Runtime` for this:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Anywhere, after the container is built:\nRootContext.Runtime\n    .Bind<IRuntimeConfig>()\n    .FromInstance(loadedConfig)\n    .AsSingle();"
    },
    {
      "type": "paragraph",
      "text": "`RootContext.Runtime` gives you direct access to the live container. `RootContext.HasInstance` lets you safely check whether it exists yet before calling it."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Injecting dependencies"
    },
    {
      "type": "paragraph",
      "text": "Zenjex supports three injection patterns. Pick the one that fits the situation."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "1. Direct resolve - `RootContext.Resolve<T>()`"
    },
    {
      "type": "paragraph",
      "text": "Works everywhere, at any time after the container is built. No base class required, no attribute needed. Just call it."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "private void Awake()\n{\n    var config = RootContext.Resolve<IGameConfig>();\n    var input  = RootContext.Resolve<IInputService>();\n}"
    },
    {
      "type": "paragraph",
      "text": "Best for: controllers, managers, or any class where you want an explicit, traceable dependency grab."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "2. Attribute injection on a plain `MonoBehaviour` - `[Zenjex]`"
    },
    {
      "type": "paragraph",
      "text": "Mark fields, properties, or inject-methods with `[Zenjex]`. **The object must already be present in the scene** when the bootstrap scene loads. Injection happens during `ProjectRootInstaller.Awake()`, before any other `Awake()` in the scene runs - so by the time your `Awake()` fires, the fields are already populated."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Attribute;\n\npublic class HUDController : MonoBehaviour\n{\n    [Zenjex] private IPlayerService _player;\n    [Zenjex] private IAudioService _audio;\n\n    private void Awake()\n    {\n        // _player and _audio are already injected here\n        _player.OnHealthChanged += UpdateHealthBar;\n    }\n}"
    },
    {
      "type": "note",
      "text": "If the object is in an **additively loaded scene**, injection happens after that scene loads - which means it arrives *after* `Awake()` has already run. In that case, the fields will be null inside `Awake()`. Zenjex will log a `ZNX-LATE` warning to make this visible. Use pattern #3 below if you need guaranteed pre-`Awake()` injection for dynamically loaded objects."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "3. `ZenjexBehaviour` - guaranteed pre-`Awake()` injection, works for runtime-instantiated objects"
    },
    {
      "type": "paragraph",
      "text": "Inherit from `ZenjexBehaviour` instead of `MonoBehaviour`. This gives the object its own `Awake()` at execution order `-100`, which means injection is guaranteed to happen before any user-level `Awake()` - even for prefabs that are `Instantiate()`-d at runtime."
    },
    {
      "type": "paragraph",
      "text": "Instead of `Awake()`, override `OnAwake()`. The injected fields are already populated when it runs."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Attribute;\nusing Zenjex.Extensions.Injector;\n\npublic class Enemy : ZenjexBehaviour\n{\n    [Zenjex] private IEnemyConfig _config;\n    [Zenjex] private IAudioService _audio;\n\n    protected override void OnAwake()\n    {\n        base.OnAwake(); // always call this first\n\n        // _config and _audio are injected - safe to use\n        _audio.Play(_config.SpawnSound);\n    }\n}"
    },
    {
      "type": "heading",
      "level": 4,
      "text": "4. Manual injection - `ZenjexRunner.InjectGameObject()`"
    },
    {
      "type": "paragraph",
      "text": "For objects created at runtime via `Instantiate()` that do not inherit from `ZenjexBehaviour`, call `InjectGameObject` immediately after instantiation. It walks the full hierarchy and injects all `[Zenjex]`-marked members."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "var enemy = Instantiate(enemyPrefab);\nZenjexRunner.InjectGameObject(enemy);"
    },
    {
      "type": "note",
      "text": "This is only needed for plain `MonoBehaviour` subclasses. `ZenjexBehaviour` handles itself automatically."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Injection timing summary"
    },
    {
      "type": "table",
      "head": [
        "Pattern",
        "Object must be in scene at load?",
        "Fields ready in `Awake()`?"
      ],
      "rows": [
        [
          "`RootContext.Resolve<T>()`",
          "No",
          "Yes (you control it)"
        ],
        [
          "`[Zenjex]` on `MonoBehaviour` (bootstrap scene)",
          "Yes",
          "Yes"
        ],
        [
          "`[Zenjex]` on `MonoBehaviour` (additive scene)",
          "Yes",
          "**No** - ZNX-LATE warning"
        ],
        [
          "`ZenjexBehaviour` + `[Zenjex]`",
          "No",
          "Yes"
        ],
        [
          "`ZenjexRunner.InjectGameObject()`",
          "No",
          "After manual call"
        ],
        [
          "`SceneInstaller` (inherits `ZenjexBehaviour`)",
          "No",
          "Yes"
        ]
      ]
    }
  ],
  "ru": [
    {
      "type": "heading",
      "level": 3,
      "text": "Жизненный цикл"
    },
    {
      "type": "paragraph",
      "text": "При каждом старте `ProjectRootInstaller` проходит по этим шагам строго в таком порядке:"
    },
    {
      "type": "list",
      "items": [
        "**`InstallBindings(builder)`** - регистрация всех глобальных сервисов. Выполняется синхронно внутри `Awake()` с порядком `-280`.",
        "**`OnContainerReady`** - первый проход инъекций `[Zenjex]` по всем загруженным сценам. Поскольку это происходит внутри `Awake()` с порядком `-280`, все последующие `Awake()` в сцене уже видят заполненные поля.",
        "**`InstallGameInstanceRoutine()`** - асинхронная настройка: загрузка Addressables, рантайм-биндинги и т.д.",
        "**`IInitializable.Initialize()`** - автоматически вызывается на каждом сервисе, реализующем `IInitializable` и зарегистрированном под этим интерфейсом. Выполняется после шага 3, до `LaunchGame()`.",
        "**`LaunchGame()`** - точка входа в игру.",
        "**`OnGameLaunched`** - второй проход инъекций `[Zenjex]`, покрывающий объекты, зависящие от биндингов, добавленных на шаге 3."
      ]
    },
    {
      "type": "heading",
      "level": 3,
      "text": "IInitializable"
    },
    {
      "type": "paragraph",
      "text": "Если сервис реализует `IInitializable`, его `Initialize()` будет вызван автоматически - после того как контейнер собран и зависимости заинжекчены, но до `LaunchGame()`. Прямой аналог `IInitializable` из Zenject."
    },
    {
      "type": "paragraph",
      "text": "Важный момент: чтобы сервис вообще нашёлся, он должен быть зарегистрирован под контрактом `IInitializable` - то есть через `BindInterfaces()` или `BindInterfacesAndSelf()`."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Lifecycle;\n\npublic class GameStateMachine : IInitializable\n{\n    private readonly StateFactory _stateFactory;\n\n    public GameStateMachine(StateFactory stateFactory) =>\n        _stateFactory = stateFactory;\n\n    public void Initialize() => Enter<BootstrapState>();\n}\n\n// В инсталлере:\nbuilder.Bind<GameStateMachine>()\n       .BindInterfacesAndSelf()\n       .AsEagerSingleton();"
    },
    {
      "type": "paragraph",
      "text": "`IInitializable` работает и в глобальном контейнере (`ProjectRootInstaller`), и в контейнере сцены (`SceneInstaller`). В сцене - сразу после сборки контейнера, до `OnInstalled()`."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Биндинг после инициализации"
    },
    {
      "type": "paragraph",
      "text": "Иногда нужно добавить что-то в контейнер уже после его сборки - скажем, конфиг, загруженный в рантайме, или сервис из `InstallGameInstanceRoutine`. Для этого есть `RootContext.Runtime`:"
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "// Из любого места, после сборки контейнера:\nRootContext.Runtime\n    .Bind<IRuntimeConfig>()\n    .FromInstance(loadedConfig)\n    .AsSingle();"
    },
    {
      "type": "paragraph",
      "text": "`RootContext.Runtime` - прямой доступ к живому контейнеру. `RootContext.HasInstance` - проверить, что контейнер уже существует, прежде чем к нему лезть."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Инъекции зависимостей"
    },
    {
      "type": "paragraph",
      "text": "Три паттерна - выбирайте по ситуации."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "1. Прямой резолв - `RootContext.Resolve<T>()`"
    },
    {
      "type": "paragraph",
      "text": "Работает в любом месте после сборки контейнера, без базового класса и без атрибутов."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "private void Awake()\n{\n    var config = RootContext.Resolve<IGameConfig>();\n    var input  = RootContext.Resolve<IInputService>();\n}"
    },
    {
      "type": "paragraph",
      "text": "Хорошо подходит для контроллеров и менеджеров, где нужен явный и легко читаемый захват зависимостей."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "2. Инъекция атрибутом на обычном `MonoBehaviour` - `[Zenjex]`"
    },
    {
      "type": "paragraph",
      "text": "Пометьте поля, свойства или inject-методы атрибутом `[Zenjex]`. **Объект должен уже быть в сцене** на момент загрузки загрузочной сцены - инъекция происходит внутри `ProjectRootInstaller.Awake()`, раньше всех остальных. К тому моменту, как сработает ваш `Awake()`, поля уже заполнены."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Attribute;\n\npublic class HUDController : MonoBehaviour\n{\n    [Zenjex] private IPlayerService _player;\n    [Zenjex] private IAudioService _audio;\n\n    private void Awake()\n    {\n        // _player и _audio уже заинжекчены\n        _player.OnHealthChanged += UpdateHealthBar;\n    }\n}"
    },
    {
      "type": "note",
      "text": "Если объект в **аддитивно загруженной сцене** - инъекция придёт уже после того, как `Awake()` отработал. Поля будут `null`. Zenjex залогирует `ZNX-LATE`. Если нужна гарантированная инъекция до `Awake()` в динамически загружаемых объектах - используйте паттерн №3."
    },
    {
      "type": "heading",
      "level": 4,
      "text": "3. `ZenjexBehaviour` - гарантированная инъекция до `Awake()`, включая рантайм-инстанциированные объекты"
    },
    {
      "type": "paragraph",
      "text": "Унаследуйтесь от `ZenjexBehaviour` вместо `MonoBehaviour`. Базовый класс запускает собственный `Awake()` с порядком `-100` - раньше любого пользовательского. Работает в том числе для префабов, созданных через `Instantiate()` в рантайме."
    },
    {
      "type": "paragraph",
      "text": "Вместо `Awake()` переопределяйте `OnAwake()` - к его вызову поля уже заполнены."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "using Zenjex.Extensions.Attribute;\nusing Zenjex.Extensions.Injector;\n\npublic class Enemy : ZenjexBehaviour\n{\n    [Zenjex] private IEnemyConfig _config;\n    [Zenjex] private IAudioService _audio;\n\n    protected override void OnAwake()\n    {\n        base.OnAwake(); // всегда вызывайте в первую очередь\n\n        // _config и _audio уже заинжекчены\n        _audio.Play(_config.SpawnSound);\n    }\n}"
    },
    {
      "type": "heading",
      "level": 4,
      "text": "4. Ручная инъекция - `ZenjexRunner.InjectGameObject()`"
    },
    {
      "type": "paragraph",
      "text": "Для объектов, созданных через `Instantiate()`, которые не наследуются от `ZenjexBehaviour`, вызовите `InjectGameObject` сразу после создания. Метод обходит всю иерархию и инжектит все поля с `[Zenjex]`."
    },
    {
      "type": "code",
      "lang": "csharp",
      "text": "var enemy = Instantiate(enemyPrefab);\nZenjexRunner.InjectGameObject(enemy);"
    },
    {
      "type": "note",
      "text": "Нужно только для обычных `MonoBehaviour`-подклассов. `ZenjexBehaviour` делает это за себя сам."
    },
    {
      "type": "heading",
      "level": 3,
      "text": "Порядок и гарантии инъекций"
    },
    {
      "type": "table",
      "head": [
        "Паттерн",
        "Объект должен быть в сцене при загрузке?",
        "Поля заполнены в `Awake()`?"
      ],
      "rows": [
        [
          "`RootContext.Resolve<T>()`",
          "Нет",
          "Да (контроль на вашей стороне)"
        ],
        [
          "`[Zenjex]` на `MonoBehaviour` (загрузочная сцена)",
          "Да",
          "Да"
        ],
        [
          "`[Zenjex]` на `MonoBehaviour` (аддитивная сцена)",
          "Да",
          "**Нет** - предупреждение ZNX-LATE"
        ],
        [
          "`ZenjexBehaviour` + `[Zenjex]`",
          "Нет",
          "Да"
        ],
        [
          "`ZenjexRunner.InjectGameObject()`",
          "Нет",
          "После ручного вызова"
        ],
        [
          "`SceneInstaller` (наследник `ZenjexBehaviour`)",
          "Нет",
          "Да"
        ]
      ]
    }
  ]
}